## 1. 编译入口与回归兼容

- [x] 1.1 在 `lenet.hpp` 增加默认 16、限定 8～16 的 `LENET_DATA_W`，保持整数位与累加器常量；Tcl 将同一宏传给设计及 Testbench，并支持独立 `LENET_HLS_WORKSPACE`。
- [x] 1.2 修正 Testbench 在 `argc == 4` 时不导出 CSV 的问题，保持默认 90% 门槛；正确传递带空格的 CSim 路径参数。
- [x] 1.3 回归原 smoke test、单 blob、blob+CSV，并用可控的低准确率输入验证 blob+CSV+0 完整导出且成功结束；检查 8/16 位编译日志和独立目录，拒绝非法位宽。

## 2. Python 精度分析

- [x] 2.1 新建 `level1/tools/precision_experiment.py`，复用既有输入校验、blob 读取、浮点参考及 CSV 能力；实现 design.md 约定的 `run`、`--python-only` 和 `report` 接口，拒绝非完整输入及非空输出目录。
- [x] 2.2 实现 Q 和层边界量化近似，保持当前网络布局、ReLU/池化/argmax 顺序；导出浮点范围统计及九档 `python_approx` 结果。
- [x] 2.3 测试正负半格舍入、饱和、各量化位置和 argmax 平局行为；确认原浮点参考及 Level 2 调用不受影响。近似模型无须模拟累加器逐 MAC 误差。

## 3. HLS 扫描与结果汇总

- [x] 3.1 实现先 16 位后 8～15 位的串行 HLS 调用，固定阈值 0，逐档保留日志、CSV、报告及状态；失败保留证据并继续，完整模式失败返回非零。
- [x] 3.2 实现逐样本 CSV 完整性检查和顶层综合报告解析，按 design.md 输出全部字段、单位及缺失值；记录输入/源码哈希、工具版本和运行条件。
- [x] 3.3 实现 90%/0.5 个百分点选型规则、资源相对变化、CSV/JSON/Markdown 汇总及两张图；`report` 从已有证据重新计算，Python-only 或任何缺档均不得正式推荐。
- [x] 3.4 用小型构造结果及报告样例测试索引错位、NaN、报告缺失、零资源基线、低准确率继续、阶段失败、98.38→97.88/97.87 的门槛边界；核实汇总不借用旧档位数据。

## 4. 复现说明与实验执行

- [x] 4.1 更新 `level1/README.md` 的数据准备、依赖、HLS 环境和三个入口命令；配置大型生成物忽略规则，明确本实验不覆盖的动态范围重缩放及进阶项目。
- [x] 4.2 运行现有 Level 1/Level 2 测试及新增关键测试，完成小样本 HLS 回归；缺少 HLS 环境时明确未验证项。
- [x] 4.3 使用完整官方 MNIST 运行九档 Python、HLS CSim 和综合；核查九档输入/工具链一致、CSV 各有 10,000 行、报告指标与汇总一致，并完成离线重新汇总检查。
- [x] 4.4 保存精简报告、图表和顶层综合证据，基于实际数据解释低位宽损失和资源收益；指定 Vivado HLS 2019.2 不可用时保留该复跑任务未完成并说明当前工具链。
- [ ] 4.5 在 Vivado HLS 2019.2 环境用独立目录复跑完整矩阵并保存结果，再标记指定工具链验证完成；不要混用开发工具链数据。

执行约定：按 1→2→3→4 推进。只有实际实现、测试或执行完对应工作才能勾选；环境不可用时交付已有代码和验证记录，保留未执行项。本变更不自动修改默认部署位宽，不提前归档。
